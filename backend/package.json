{
  "name": "curion-agent-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check api/agent/map-form.js && node --check api/profile/ingest.js && node --check api/_lib/semantic-profile.js && node --check api/_lib/gemini.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
